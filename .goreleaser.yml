# GoReleaser configuration — https://goreleaser.com
# Builds macOS + Linux binaries (amd64 + arm64), publishes a GitHub release,
# and pushes a Homebrew formula to the plumbkit/homebrew-plumb tap.
#
# Plumb is pure-Go (no CGo), so cross-compilation is clean: CGO_ENABLED=0.
# Run locally with `goreleaser release --snapshot --clean` to test without publishing.
version: 2

project_name: plumb

before:
  hooks:
    - go mod tidy

builds:
  - id: plumb
    main: ./cmd/plumb
    binary: plumb
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      # Mirrors the Makefile's version and provenance injection; GoReleaser
      # supplies the tag and the commit.
      #
      # Dirtiness is MEASURED via .IsGitDirty, never asserted. A `goreleaser
      # release` refuses to run on a dirty tree, so hard-coding false would be
      # true for a real release — but `--snapshot` skips that validation, and
      # snapshot is this project's documented local dry-run path. A dirty
      # snapshot would then claim a clean tree: precisely the lie this stamping
      # exists to prevent. Verified against goreleaser v2.17.1: a dirty snapshot
      # renders .IsGitDirty true, a clean release build false.
      #
      # BuildChannel likewise distinguishes a snapshot from a release, so a local
      # dry-run binary cannot self-identify as an official build. A snapshot maps
      # to "dev" rather than a fourth channel name: the field answers "is this an
      # official release?", and a snapshot's answer is the same no as the
      # Makefile's.
      #
      # No build timestamp is stamped, deliberately — it would make builds
      # non-reproducible.
      - >-
        -s -w
        -X github.com/plumbkit/plumb/internal/cli.Version={{ .Version }}
        -X github.com/plumbkit/plumb/internal/cli.Revision={{ .FullCommit }}
        -X github.com/plumbkit/plumb/internal/cli.RevisionDirty={{ .IsGitDirty }}
        -X github.com/plumbkit/plumb/internal/cli.BuildChannel={{ if .IsSnapshot }}dev{{ else }}release{{ end }}

archives:
  - id: plumb
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "merge conflict"
      - "Merge pull request"
      - "Merge branch"
  groups:
    - title: Features
      regexp: '^feat'
      order: 0
    - title: Fixes
      regexp: '^fix'
      order: 1
    - title: Others
      order: 999

brews:
  - name: plumb
    repository:
      owner: plumbkit
      name: homebrew-plumb
      # PAT with `repo` scope on the tap repo, provided by the release workflow.
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/plumbkit/plumb"
    description: "LSP-backed MCP server with crash-safe, concurrent writes for coding agents."
    license: "MIT"
    # Where the formula installs from within the tap repo.
    directory: Formula
    commit_author:
      name: goreleaser-bot
      email: hello@getplumb.sh
    install: |
      bin.install "plumb"
    test: |
      system "#{bin}/plumb", "version"

release:
  github:
    owner: plumbkit
    name: plumb
  draft: false
  prerelease: auto # tags like v0.9.0-rc1 are marked pre-release automatically
  footer: |
    ## Install

    **Homebrew** (macOS + Linux):

    ```sh
    brew install plumbkit/plumb/plumb
    ```

    **Direct download:** grab the archive for your OS/arch below, extract it, and
    put the `plumb` binary on your `PATH`.

    | Platform | Archive |
    |---|---|
    | macOS, Apple Silicon (M-series) | `plumb_{{ .Version }}_darwin_arm64.tar.gz` |
    | macOS, Intel | `plumb_{{ .Version }}_darwin_amd64.tar.gz` |
    | Linux, Intel/AMD 64-bit | `plumb_{{ .Version }}_linux_amd64.tar.gz` |
    | Linux, ARM 64-bit | `plumb_{{ .Version }}_linux_arm64.tar.gz` |

    Verify your download against `checksums.txt`.

    > **macOS note:** these binaries are not yet code-signed. On a direct download
    > Gatekeeper may block the first run — clear the quarantine flag once:
    > `xattr -dr com.apple.quarantine ./plumb`. Homebrew installs are unaffected.
